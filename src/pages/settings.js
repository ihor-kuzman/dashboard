import React from 'react';

import Wrapper from '../components/wrapper';

export default function Settings() {
    return (
        <Wrapper
            title="Settings"
        >
            Settings
        </Wrapper>
    );
}
